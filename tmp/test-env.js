
const title = 'iPhone 15 Pro';

async function testSuggest() {
    console.log('Testing suggest for:', title);
    // Since I can't easily run the Next.js API route as a function here without complex imports,
    // I'll simulate the Gemini call if I can, or just check the code again.
    
    // Actually, I'll try to find any environment issues.
    console.log('API Key present:', !!process.env.GEMINI_API_KEY);
    
    // I'll try to use the same logic in a script to see if it even runs.
}

testSuggest();
